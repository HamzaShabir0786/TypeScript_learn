"use strict";
function half() {
    let cooked = "pizza, egge, ";
    console.log(cooked);
}
half();
function fast() {
    let faster = 4 + 8;
    return faster;
}
let f = fast();
console.log(f);
function making(oil, butter, salt) {
    return oil + butter + salt;
}
let ready = making(2, 2, 2);
console.log(ready);
